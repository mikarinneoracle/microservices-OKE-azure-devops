apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: node-db-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: node-clusterip
              port:
                number: 8080
